<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 
    PERMISSIONS EXPLANATION:
    
    1. SYSTEM_ALERT_WINDOW - Required to display floating overlay on top of other apps
       Must be granted manually by user via Settings → Apps → Special access → Display over other apps
    
    2. RECORD_AUDIO - Required for speech recognition via microphone
       Runtime permission, requested when user first taps mic button
    
    3. FOREGROUND_SERVICE - Required to keep the overlay service running
       Automatically granted, no user action needed
    
    4. FOREGROUND_SERVICE_SPECIAL_USE - Required on Android 14+ for services that don't fit
       predefined categories. We use this for our accessibility-related overlay.
    -->
    
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Android 13+ requires runtime permission to show notifications (including ongoing/foreground notifications) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- Internet may be needed for online speech recognition -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Keep device awake during recording to prevent interruption -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".WhisperTypeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhisperType"
        tools:targetApi="34">
        
        <!-- Main Activity: Onboarding and permission setup -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WhisperType">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 
        ACCESSIBILITY SERVICE
        
        This is the core component that:
        - Appears in Settings → Accessibility → WhisperType
        - Receives accessibility shortcut events (volume button double-press)
        - Can access focused input fields for text insertion
        
        The meta-data points to our accessibility service configuration XML.
        -->
        <service
            android:name=".service.WhisperTypeAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_label"
            android:description="@string/accessibility_service_description"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="accessibility_keepalive" />
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 
        OVERLAY SERVICE
        
        Foreground service that manages the floating mic overlay.
        Uses SYSTEM_ALERT_WINDOW to display on top of other apps.
        
        On Android 14+, we specify foregroundServiceType as "specialUse" since
        our service doesn't fit the predefined categories (media, location, etc.)
        -->
        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="accessibility_overlay" />
        </service>

    </application>

</manifest>
