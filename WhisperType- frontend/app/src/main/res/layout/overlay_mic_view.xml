<?xml version="1.0" encoding="utf-8"?>
<!--
OVERLAY MIC VIEW LAYOUT

This is the floating overlay that appears when the accessibility shortcut is triggered.

Design principles:
1. Compact: Small enough not to obstruct the screen
2. Non-focusable: Uses FLAG_NOT_FOCUSABLE so it doesn't steal focus from text fields
3. Draggable: Users can move it around the screen
4. Clear visual states: Idle, listening, processing, error

The layout consists of:
- A draggable circular container
- A mic button in the center
- A status text below
- A close (X) button in the corner
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/overlay_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <!-- Main container card -->
    <LinearLayout
        android:id="@+id/overlay_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:background="@drawable/overlay_background"
        android:padding="16dp"
        android:elevation="8dp">

        <!-- Close button (top right corner) -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
            
            <ImageButton
                android:id="@+id/btn_close"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="end"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_close"
                android:contentDescription="Close overlay"
                android:scaleType="centerInside"
                tools:ignore="TouchTargetSizeCheck" />
        </FrameLayout>

        <!-- Mic button - main interaction element -->
        <FrameLayout
            android:id="@+id/mic_button_container"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/mic_button_background">

            <ImageView
                android:id="@+id/ic_mic"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_mic"
                android:contentDescription="Microphone button" />

            <!-- Loading indicator for processing/transcribing state -->
            <ProgressBar
                android:id="@+id/progress_indicator"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:layout_gravity="center"
                android:indeterminateTint="#6366F1"
                android:visibility="gone"
                tools:visibility="visible" />
        </FrameLayout>

        <!-- Status text indicator -->
        <TextView
            android:id="@+id/tv_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/overlay_ready"
            android:textColor="@color/overlay_text"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxWidth="140dp"
            android:gravity="center"
            android:ellipsize="end"
            android:maxLines="2" />

        <!-- Recognized text preview (shows partial results) -->
        <TextView
            android:id="@+id/tv_preview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:textColor="@color/overlay_text"
            android:textSize="11sp"
            android:alpha="0.85"
            android:maxWidth="160dp"
            android:gravity="center"
            android:ellipsize="end"
            android:maxLines="3"
            android:visibility="gone"
            tools:text="Hello world..."
            tools:visibility="visible" />

        <!-- Copy to Clipboard button (shown when no text field is focused) -->
        <Button
            android:id="@+id/btn_copy"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_marginTop="12dp"
            android:paddingLeft="24dp"
            android:paddingRight="24dp"
            android:text="@string/btn_copy_clipboard"
            android:textColor="@color/on_primary"
            android:textSize="12sp"
            android:background="@drawable/copy_button_background"
            android:textAllCaps="false"
            android:visibility="gone"
            tools:visibility="visible" />

    </LinearLayout>

</FrameLayout>
