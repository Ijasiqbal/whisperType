<?xml version="1.0" encoding="utf-8"?>
<!--
AUTO-SEND WARNING DIALOG - Custom overlay dialog

This appears as a separate overlay window when user enables auto-send for the first time.
Uses solid opaque background to prevent transparency overlap issues.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/warning_dialog_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC000000"
    android:clickable="true"
    android:focusable="true">

    <!-- Dialog container with opaque background -->
    <LinearLayout
        android:id="@+id/warning_dialog_container"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:background="@drawable/warning_dialog_background"
        android:paddingHorizontal="20dp"
        android:paddingTop="18dp"
        android:paddingBottom="16dp"
        android:elevation="24dp">

        <!-- Header: Icon + Title -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="10dp">

            <ImageView
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:src="@drawable/ic_send"
                android:contentDescription="Auto-send"
                android:tint="#FF9500"
                android:layout_marginEnd="10dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Enable Auto-Send?"
                android:textColor="#FFFFFF"
                android:textSize="17sp"
                android:textStyle="bold"
                android:letterSpacing="0.01" />

        </LinearLayout>

        <!-- Message -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Messages will be sent automatically after transcription.\n\nYou won't be able to review before sending."
            android:textColor="#B0FFFFFF"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.3"
            android:layout_marginBottom="18dp" />

        <!-- Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <TextView
                android:id="@+id/btn_cancel"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:gravity="center"
                android:text="Cancel"
                android:textColor="#80FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold"
                android:paddingHorizontal="16dp"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:layout_marginEnd="8dp" />

            <TextView
                android:id="@+id/btn_enable"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:gravity="center"
                android:text="Enable"
                android:textColor="#FFFFFF"
                android:textSize="14sp"
                android:textStyle="bold"
                android:paddingHorizontal="24dp"
                android:background="@drawable/copy_button_liquid_pill" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
